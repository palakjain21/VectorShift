export const SubmitButton = () => {

    return (
        <div style={{display: 'flex', alignItems: 'center', justifyContent: 'center'}}>
            <button type="submit" className="bg-primary-800 text-white p-2 rounded-md hover:bg-primary-900">Submit</button>
        </div>
    );
}
